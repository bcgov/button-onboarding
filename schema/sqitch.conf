[core]
	engine = pg
	# plan_file = sqitch.plan
	# top_dir = .
# [engine "pg"]
	# target = db:pg:
	# registry = sqitch
	# client = psql
[target "todo_app_db"]
    uri = db:pg:todo_app_db
[engine "pg"]
    target = todo_app_db
[deploy]
	verify = true
[rebase]
	verify = true
